from setuptools import setup, find_packages

setup(
    name="star2gps",
    version="0.0.1",
    description="Minimal packaging for star2gps",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    python_requires=">=3.7",
)